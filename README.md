# API-Books-Top100
API desarrollada en Python con FastAPI que permite consultar, modificar, eliminar y agregar libros en una lista de los 100 mejores títulos. Incluye un cliente interactivo en consola para realizar las operaciones y persistencia local del dataset en formato JSON.
